// slider js code
let config = {
    type: 'loop',
    perPage: 1,
    arrows: true,
    pagination: false,
};

new Splide('._testimonial_agency_slider', config).mount();

// wow js code
new WOW().init();
